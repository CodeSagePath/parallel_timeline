name: parallel_timeline
description: A Flutter package for building parallel, dual-column timeline interfaces.
version: 1.0.0
repository: https://github.com/CodeSagePath/parallel_timeline
homepage: https://github.com/CodeSagePath/parallel_timeline
topics:
  - timeline
  - schedule
  - calendar
  - widgets

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
